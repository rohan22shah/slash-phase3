name: slash-phase3
language: python
python:
  - "3.6"
  - "3.7"
before_install:
  - python -m pip install --upgrade pip
install:
  - pip install -r requirements.txt
before_script:
  - sudo apt-get update
sudo: required
script:
  - cd tests 
  - pytest 
